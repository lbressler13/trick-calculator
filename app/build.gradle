plugins {
    id 'com.android.application'
    id 'kotlin-android'
    id "org.jlleitschuh.gradle.ktlint" version "10.3.0" // ktlint
}
apply plugin: 'kotlin-android'

android {
    compileSdk 31

    defaultConfig {
        applicationId "com.example.trickcalculator"
        minSdk 30
        targetSdk 31
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    flavorDimensions "type"
    productFlavors {
        dev {
            dimension "type"
            versionNameSuffix "-dev"
            applicationId "com.example.trickcalculator.dev"
        }

        // labelled as complete b/c final can't be used
        complete {
            dimension "type"
            versionNameSuffix "-final"
        }
    }

    sourceSets {
        dev {
            java.srcDirs = ['src/dev/java']
            resources.srcDirs = ['src/dev/res']
        }

        complete {
            java.srcDirs = ['src/final/java']
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    def androidxCoreVersion = "1.7.0"
    def appcompatVersion = "1.4.1"
    def constraintLayoutVersion = "2.1.3"
    def lifecycleVersion = "2.4.1"
    def materialVersion = "1.5.0"
    def navigationVersion = "2.5.1"

    def mockitoVersion = "4.3.1"
    def mockitoKotlinVersion = "4.0.0"

    def exactNumbersVersion = "0.0.9"
    def kotlinutilsVersion = "0.0.5"

    // TODO cleanup/update versions
    implementation "androidx.core:core-ktx:$androidxCoreVersion"
    implementation "androidx.appcompat:appcompat:$appcompatVersion"
    implementation "com.google.android.material:material:$materialVersion"
    implementation "androidx.constraintlayout:constraintlayout:$constraintLayoutVersion"
    implementation "androidx.lifecycle:lifecycle-livedata-ktx:$lifecycleVersion"
    implementation "androidx.lifecycle:lifecycle-viewmodel-ktx:$lifecycleVersion"
    implementation "androidx.navigation:navigation-fragment-ktx:$navigationVersion"
    implementation "androidx.navigation:navigation-ui-ktx:$navigationVersion"
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlinVersion"

    // testing
    testImplementation "junit:junit:4.13.2"
    testImplementation "org.mockito:mockito-core:$mockitoVersion"
    testImplementation "org.mockito.kotlin:mockito-kotlin:$mockitoKotlinVersion"
    androidTestImplementation "androidx.test.ext:junit:1.1.3"
    androidTestImplementation "androidx.test.espresso:espresso-core:3.4.0"

    // local libraries
    implementation files("libs/exact-numbers-${exactNumbersVersion}.jar")
    implementation files("libs/kotlin-utils-${kotlinutilsVersion}.jar")
}
